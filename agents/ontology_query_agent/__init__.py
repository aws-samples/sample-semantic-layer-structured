"""
Virtual Knowledge Graph Query Agent Package
"""
# Do NOT import from main here: the CMD uses `python -m ontology_query_agent.main`
# which already initializes the package, causing double module execution and the
# "found in sys.modules" RuntimeWarning that breaks BedrockAgentCoreApp startup.
